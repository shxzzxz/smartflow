import 'package:flutter/material.dart';

abstract final class AppElevation {
  static const double surfaceShadowOpacity = 0.06;
  static const double surfaceShadowBlur = 12;
  static const Offset surfaceShadowOffset = Offset(0, 2);
}
