# 架构决策

本文档登记 SmartFlow 在演进过程中做出的、影响跨模块协作或长期演进方向的关键决策。

每条决策只在本文写一条**摘要**（约束 + 一句话理由），完整背景、含义与反例放到对应的 `08.x` 子文档。新增条目时优先沿用已有编号顺序，不重排历史编号。

## 何时新增条目

- 决策**约束的是跨层 / 跨模块边界**（例如：A 层不得依赖 B 层、B 模块通过 X 协议接入核心层）
- 决策一旦反悔会引起**广泛重构**，未来贡献者需要先理解再扩展
- 仅影响单文件 / 单模块的局部权衡**不**进本文档，写到代码注释或对应模块的 README 即可

## 决策索引

| 编号 | 标题 | 摘要 | 详情 |
| :--- | :--- | :--- | :--- |
| 08.1 | 业务域划分与账务核心独立性 | `lib/domain/` 与 `lib/data/` 按业务域划分子目录，`accounting`（账务核心）作为 Shared Kernel；其它业务域单向依赖账务核心的对外面（`entities / enums / services`），兄弟业务域之间不互相 import；区分流图原语（进核心）与独立业务域 | [08.1 业务域划分与账务核心独立性](08.1%20业务域划分与账务核心独立性.md) |
| 08.2 | 通用 UI 与业务域接入协议 | 通用 UI 通过交易 `owner_*` 元数据预解析 `TransactionActionPolicy`；动作分 universal（policy 接管）与 purpose-bound（UI 按 purpose 渲染）；TransactionActionPolicy 整套位于 `features/transactions/` 集成层，不属于任何业务域 | [08.2 通用 UI 与业务域接入协议](08.2%20通用UI与业务域接入协议.md) |
| 08.3 | 字段编辑三态协议 | 跨层 partial update 统一用 `Patch<T>?`（null=不改 / `Patch.set` / `Patch.clear`）表达三态，禁止配对 `clearXxx: bool` 与层间翻译 | [08.3 字段编辑三态协议](08.3%20字段编辑三态协议.md) |
| 08.4 | 仓储层与读路径分层 | 读路径分三层：能力(repository / service 原子方法,无业务字面量) → 领域 read model(场景化会计视图,无 UI 装饰) → UI 层 view model(页面状态 + UI 派生);数据编排归 service,响应式由 repository 暴露 `watchChanges()` 抽象,domain 不依赖 drift `tableUpdates` | [08.4 仓储层与读路径分层](08.4%20仓储层与读路径分层.md) |

## 子文档模板

每个 `08.x` 子文档建议采用以下结构，便于横向比较：

- **背景**：触发决策的问题、之前的状态
- **决策**：用一句陈述句概括做什么 / 不做什么
- **含义**：对代码组织、调用方向、UI 路径的影响
- **适用案例**：决策落地的具体场景
- **反例**：违反本决策的常见诱惑性方案，以及为什么不取
- **演进规则**：未来出现相似场景时如何沿用

---

**文档版本**：v0.7
