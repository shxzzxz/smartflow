# 入账时间由账务核心持有

账务核心的每笔交易同时持有非空的交易时间 `occurred_at` 与入账时间 `posted_at`，创建时后者默认等于前者，两者之后独立维护且不约束先后。交易列表、流水、收支和预算统计继续以 `occurred_at` 为口径；信贷域只消费 `posted_at` 的日期部分决定信用账单周期，不在交易更新时同步刷新账单投影。`posted_at` 作为当前交易的基础信息直接更新，历史更正版本与已冲销交易保持只读。
